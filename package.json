{
  "name": "numis-via-coinprint",
  "version": "14.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/analyze.js && node --check api/catalogue-search.js && node --check scripts/sync-open-catalogues.mjs && node --test",
    "catalogue:sync": "node scripts/sync-open-catalogues.mjs"
  }
}
